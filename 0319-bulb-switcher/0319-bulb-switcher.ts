function bulbSwitch(n: number): number {
    // let arr=Array(n).fill(0);

    // for( let i=1;i<=n;i++){
        
    //    for( let j=i-1;j<n;j+=i){
    //      arr[j]= arr[j] === 0? 1:0;
    //    }

    // }

    // return arr.filter((bulb)=> bulb === 1).length;
    //  time limit excedded 
        return Math.floor (Math.sqrt (n));


};